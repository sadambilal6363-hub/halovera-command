# اكتشاف فرص مليحة

قسم «اكتشاف أنشطة مليحة» في رادار الفرص يجلب عند الطلب أنشطة من بيانات OpenStreetMap العامة عبر Overpass API حول مليحة والقرى القريبة (نطاق 7 كلم). يحد المصدر النتيجة إلى 120 عنصرًا ويمنع إعادة الاستعلام خلال ست ساعات. قد تكون النتائج ناقصة أو قديمة؛ غياب رابط موقع في البيانات لا يثبت عدم وجود موقع حقيقي. يُعرض رابط المصدر وعبارة نسب البيانات إلى مساهمي OpenStreetMap. لا يجلب هذا المسار قوائم جهات حكومية أو يشغّل مراسلة تلقائية.

تُحفظ النتائج في D1 ضمن `opportunity_discoveries`. الضغط على «جهّز عرضًا» ينشئ مسودة عرض في `/content-studio` بناءً على البيانات الموجودة. عند توفر `OPENAI_API_KEY` و`OPENAI_MODEL` يُجهّز النموذج مسودة؛ وإلا تُنشأ صياغة عامة قابلة للتحرير. لا تُعتبر أرقام التواصل أو الاحتياج الرقمي مؤكدة قبل مراجعتك. إرسال العرض للعميل خطوة مستقلة، تخضع لقرارك ولطريقة التواصل المتاحة.

يحمي واجهة البحث ومدخل المسودات تسجيل المالك الموجود في صندوق واتساب. صفحة رادار الفرص الأصلية تعرض بيانات `leads` من دون حماية على مستوى الصفحة؛ اضبط Cloudflare Access لمركز القيادة كاملًا قبل استخدام بيانات عملاء حقيقية. يبدأ البحث تلقائيًا عند فتح الرادار بعد الدخول إذا مضت ست ساعات على البحث السابق، ويمكن بدء البحث بالزر. لم تُضف جدولة مستقلة للعمل في الخلفية أثناء إغلاق الموقع؛ تحتاج إلى جدولة Worker ومصدر بيانات يلائم حجم الطلب الفعلي.
