# استوديو محتوى هلوفيرا

صفحة `/content-studio` تُجهّز عروضًا للعملاء وبوستات ونصوص إعلانات من معلومات مؤكدة يدخلها المالك. يمكن أيضًا كتابة المسودة يدويًا. يحميها تسجيل الدخول نفسه المستخدم في صندوق واتساب؛ يحتاج `INBOX_ADMIN_PASSWORD` و`INBOX_SESSION_SECRET`، مع حماية مركز القيادة كله بواسطة Cloudflare Access قبل التعامل مع معلومات عملاء حقيقية.

توليد النصوص يحتاج `OPENAI_API_KEY` و`OPENAI_MODEL` في أسرار الخادم، بفوترة API مستقلة عن اشتراك ChatGPT. تحفظ المسودات في Cloudflare D1. تعديل العنوان أو النص يلغي اعتماد النسخة السابقة. عند اعتماد المسودة تُحفظ بصمة المحتوى المعتمد؛ ولن يسمح مسار النشر بمحاولة نشر نص مختلف عن المعتمد.

**قنوات الموقع وفيسبوك وإنستغرام غير موصولة حاليًا.** زر «انشر» لها يعيد رسالة صريحة بأن القناة غير مربوطة ولا يضع علامة «تم النشر». يلزم توصيل كل حساب نشر عبر API رسمي والحصول على إذنه، وحفظ إثبات الاستجابة من القناة قبل تغيير حالة المسودة إلى منشورة. الإعلانات المدفوعة تحتاج خطوة اعتماد مستقلة للميزانية والحملة قبل تشغيلها. أمر «انشر» عبر المحادثة غير موصول بعد؛ يلزم تحديد المسودة والقناة واعتماد النسخة قبل إرسال أي أمر نشر.

## واتساب

- «رسالة واتساب لعميل»: يُحفظ رقم المستلم برمز الدولة في النسخة المعتمدة ولا يمكن تغييره دون إنشاء مسودة جديدة. بعد اعتماد النص، يؤكد المالك أن العميل طلب هذا العرض، أو سبق أن وافق على تلقي عروض تسويقية، ثم يضغط «إرسال عبر واتساب». يحتاج المسار رقمًا حقيقيًا مسجلًا في Cloud API أو تشغيلًا مشتركًا، وأسرار `WHATSAPP_ACCESS_TOKEN` و`WHATSAPP_PHONE_NUMBER_ID`، ورسالة واردة من المستلم خلال آخر 24 ساعة في صندوقنا. أي إرسال خارج تلك الفترة يحتاج قالبًا معتمدًا؛ هذا المسار لا ينشئ القوالب. يوقف النظام إعادة المحاولة التلقائية إذا كانت نتيجة Meta غامضة، منعًا لتكرار الرسالة. رد Meta بمعرف رسالة يعني قبولها مبدئيًا، وليس إثبات قراءتها أو وصولها النهائي.
- «حالة واتساب»: يُعد النظام نص الحالة ويتيح نسخه بعد الاعتماد، ثم ينشره المالك من **واتساب أعمال ← التحديثات ← إضافة حالة**. لا يدعي النظام نشر الحالة تلقائيًا؛ لا يوفر ربط Cloud API المستخدم هنا مسارًا مؤكدًا لنشر الحالة.
- النشر إلى الموقع وفيسبوك وإنستغرام ما زال غير موصول. لا يوجد أمر نشر صوتي/نصي من داخل ChatGPT أو واتساب حتى الآن.
