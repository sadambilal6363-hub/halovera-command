# ربط واتساب هلوفيرا بمركز القيادة

المسار `/api/whatsapp/webhook` يتحقق من تحدي Meta، يتحقق من توقيع الأحداث، ويرد تلقائيًا على الرسائل النصية الواردة إلى رقم هلوفيرا المحدد. الردود الحالية ترحيبية وتوجّه العميل إلى خدمات المواقع والإعلانات والهوية؛ لا تستخدم الذكاء الاصطناعي ولا ترسل حملات تسويقية.

## قبل تفعيل الردود

1. أضف رقم هلوفيرا الحقيقي إلى WhatsApp Business Account في Meta وتأكد من اعتماده. الرقم الأمريكي الاختباري لا يصلح للتشغيل.
2. انشر هذا التغيير على Cloudflare Worker المرتبط بالمستودع.
3. أضف هذه القيم كأسرار/متغيرات للخادم في Cloudflare، وليس في واجهة الموقع أو GitHub:
   - `WHATSAPP_VERIFY_TOKEN`: عبارة سرية تنشئها أنت، وتكتب **القيمة نفسها** في حقل «تحقق من الرمز» لدى Meta.
   - `META_APP_SECRET`: مفتاح التطبيق من Meta، للتحقق من `X-Hub-Signature-256`.
   - `WHATSAPP_ACCESS_TOKEN`: رمز الوصول الدائم لحساب واتساب.
   - `WHATSAPP_PHONE_NUMBER_ID`: معرف رقم هلوفيرا الحقيقي، وليس رقم الهاتف النصي ولا معرف الرقم الاختباري.
   - `WHATSAPP_GRAPH_VERSION`: اختياري؛ الافتراضي `v23.0`. راجع نسخة Graph API المدعومة في حساب Meta عند الإعداد.
   - `WHATSAPP_AUTO_REPLY_ENABLED`: اضبطها على `true` فقط بعد إتمام التسجيل والتحقق من الإعدادات.
4. ضع رابط النشر النهائي متبوعًا بـ`/api/whatsapp/webhook` في حقل **عنوان URL للاستدعاء** لدى Meta. ضع `WHATSAPP_VERIFY_TOKEN` في حقل التحقق، ثم اضغط «تحقق وحفظ». اشترك في حدث `messages` لرقم/حساب واتساب.
5. أرسل رسالة من هاتف آخر إلى رقم هلوفيرا الحقيقي. تحقق من وصول رد واحد مناسب؛ ثم كرر الطلب نفسه في سجلاتك إن لزم للتأكد من منع التكرار.

لا تضع رمز الوصول أو مفتاح التطبيق في صور أو رسائل أو ملفات المستودع. يضيف المسار جدول `whatsapp_processed_messages` إلى D1 عند أول رسالة نصية مؤهلة لمنع الرد المكرر عند إعادة محاولة Meta. إذا فشل الإرسال إلى Meta، يحذف قيد الرسالة كي يمكن إعادة المحاولة. لا تفعّل الردود قبل وجود رقم حقيقي وبيانات اعتماد صحيحة.

## صندوق المحادثات والرد اليدوي

- افتح `/whatsapp-inbox` بعد النشر. أضف `INBOX_ADMIN_PASSWORD` (كلمة مرور فريدة من 24 حرفًا أو أكثر) و`INBOX_SESSION_SECRET` (قيمة عشوائية مختلفة من 32 حرفًا أو أكثر) كأسرار في Cloudflare. إذا لم تضفهما، الصندوق لا يسمح بالدخول. لا تضعهما في GitHub أو الواجهة العامة.
- يخزن Webhook الرسائل الواردة في D1 بمجرد صحة توقيع Meta، حتى عند تعطيل الردود الآلية. لا تظهر الرسائل القديمة الموجودة في تطبيق واتساب أعمال قبل نقل الرقم. تظهر الرسائل النصية في الصندوق، وتظهر الوسائط كإشارة فقط؛ لا يمكن عرض الملفات في هذه النسخة.
- استخدم زر «تولّي المحادثة يدويًا» لإيقاف الرد الآلي لهذا العميل. إرسال رد من الصندوق يحوّل المحادثة تلقائيًا إلى وضع يدوي. زر «إعادة الرد الآلي» يعيد الردود على رسائل العميل الجديدة.
- يعرض الصندوق آخر 100 محادثة وآخر 100 رسالة في كل محادثة. يحتاج المستخدم إلى تسجيل الدخول مجددًا كل 7 أيام.
- يرسل الرد اليدوي عبر Cloud API للرقم المسجّل نفسه؛ الرسائل اليدوية خارج نافذة 24 ساعة من آخر رسالة للعميل قد تتطلب قالبًا معتمدًا، وتظهر رسالة فشل بدل ادعاء نجاحها. لا تكرر الإرسال عند فشل تأكيد Meta حتى تتحقق من وصول الرسالة للعميل.
- قبل حذف حساب واتساب أعمال الحالي، انشر الصندوق والكود، أضف الأسرار، تأكد من إمكانية فتح صفحة الدخول، جهّز نسخة من محادثاتك المهمة، ثم انقل الرقم وسجّل Webhook واشترك في `messages`. جرّب عميلًا حقيقيًا والرد اليدوي قبل توجيه الزيارات إلى الرقم. نشر الكود وحده لا ينقل الرقم.
- المشروع لا يملك نظام دخول عام؛ حماية صندوق واتساب محلية له فقط. بقية شاشات مركز القيادة وواجهات API الأخرى قد تكون مكشوفة إذا كان الدومين عامًا، لذلك اضبط وصول Cloudflare Access على مستوى تطبيق مركز القيادة قبل استخدامه لبيانات عملاء فعلية.

## اقتراح الرد الذكي وربط رادار الفرص

- أضف `OPENAI_API_KEY` سرًا في Cloudflare، واضبط `OPENAI_MODEL` على نموذج يدعم Structured Outputs من Responses API. استعمال OpenAI API يحتاج حساب API وفوترة منفصلين عن اشتراك ChatGPT.
- اضبط `WHATSAPP_AI_ENABLED=true` ليحلل النظام الرسالة الواردة (نص مقتطع بحد 1500 حرف)، ويحفظ ملخصًا واقتراح رد يظهر في صندوق المحادثات. عند رصد طلب تجاري جديد يسجله كفرصة في جدول `leads` الذي يرتبه رادار الفرص. لا يكتشف الرادار شركات خارجية بنفسه.
- يبقى إرسال النص الذكي **معطّلًا افتراضيًا**؛ عند تفعيل `WHATSAPP_AUTO_REPLY_ENABLED=true` يستخدم النظام ردوده المحددة، وبعد اختبار جودة الاقتراحات يمكن ضبط `WHATSAPP_AI_AUTO_SEND=true` لإرسال النص الذكي للعملاء. تستمر حدود نافذة واتساب ومراجعة الرسائل والقدرة على التحويل البشري.
- إذا تعذر اتصال AI أو لم يوجد المفتاح، يستمر الرد المحدد بالكلمات. بيانات المحادثة المرسلة إلى خدمة AI تخرج من Cloudflare إلى مزود النموذج؛ راجع موافقة العملاء وسياسة الخصوصية قبل التشغيل الفعلي. لا تُدرج مفاتيح API في الموقع أو GitHub.
- يمكن استخدام واتساب أعمال الحالي دون أي API لاستقبال رسائل من زر الموقع والرد يدويًا مع رسالة ترحيب. الربط البرمجي الذي يحلل كل رسالة ويتصرف بناءً عليها يحتاج Cloud API / Webhook أو خدمة تدعم التشغيل المتزامن مع تطبيق واتساب أعمال.
