import {getCloudflareContext} from "@opennextjs/cloudflare";import{NextResponse}from"next/server";export const dynamic="force-dynamic";
export async function POST(r){try{const b=await r.json();const amount=Number(b.amount||0);if(amount<=0)return NextResponse.json({ok:false,error:"amount required"},{status:400});const{env}=await getCloudflareContext({async:true});const x=await env.DB.prepare("INSERT INTO expenses(project_id,category,amount,description,expense_date,created_at) VALUES(?,?,?,?,?,CURRENT_TIMESTAMP)").bind(b.project_id||null,b.category||null,amount,b.description||null,b.expense_date||null).run();return NextResponse.json({ok:true,id:x.meta?.last_row_id},{status:201})}catch(e){return NextResponse.json({ok:false,error:String(e)},{status:500})}}